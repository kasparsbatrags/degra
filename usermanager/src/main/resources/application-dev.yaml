spring:
  config:
    import: optional:classpath:application-shared-dev.yaml

server:
  port: 8296

app:
  security:
    allowed-origins: "http://localhost:8080"

# OpenAPI Server Configuration
springdoc:
  server:
    dev:
      url: http://localhost:8080