stages:
  - test
  - build
  - deploy-test
  - deploy-prod

variables:
  DOCKER_TLS_CERTDIR: "/certs"
  DOCKER_HOST: tcp://docker:2376
  DOCKER_TLS_VERIFY: 1
  DOCKER_CERT_PATH: "$DOCKER_TLS_CERTDIR/client"
  SONAR_USER_HOME: "${CI_PROJECT_DIR}/.sonar"
  GIT_DEPTH: "0"

services:
  - docker:20-dind

# Kopīgais skripts, kas tiks izmantots vairākos darbos
.maven-setup: &maven-setup
  image: maven:3.9.5-eclipse-temurin-21
  cache:
    paths:
      - .m2/repository

# Qodana koda kvalitātes pārbaude
qodana:
  stage: test
  image:
    name: jetbrains/qodana-jvm
    entrypoint: [""]
  variables:
    QODANA_TOKEN: $qodana_token
  script:
    - qodana --save-report --results-dir=$CI_PROJECT_DIR/.qodana
  only:
    - master
    - main
    - merge_requests
  when: manual

# Testēšanas darbi
usermanager-test:
  <<: *maven-setup
  stage: test
  script:
    - mvn clean test -pl usermanager -am
  artifacts:
    reports:
      junit: usermanager/target/surefire-reports/TEST-*.xml

company-test:
  <<: *maven-setup
  stage: test
  script:
    - mvn clean test -pl company -am
  artifacts:
    reports:
      junit: company/target/surefire-reports/TEST-*.xml

# SonarCloud integrācija
sonarcloud-check:
  stage: test
  image: maven:3.9.5-eclipse-temurin-21
  script:
    - mvn verify sonar:sonar -Dsonar.projectKey=kaspars.batrags_degra -Djavafx.version=17
  only:
    - merge_requests
    - master
    - main
    - develop
  when: manual

# Būvēšanas darbi
# Core moduļa būvēšana
core-build:
  <<: *maven-setup
  stage: build
  script:
    - mvn clean install -pl core -am -DskipTests -Dsonar.skip=true
  artifacts:
    paths:
      - core/target/*.jar
  only:
    - branches
    - test
    - master

usermanager-maven-build:
  <<: *maven-setup
  stage: build
  script:
    - mvn clean package -pl usermanager -am -DskipTests
  artifacts:
    paths:
      - usermanager/target/*.jar
      - core/target/*.jar
  dependencies:
    - core-build
  needs:
    - usermanager-test
    - core-build
  only:
    - test
    - master

usermanager-docker-build:
  stage: build
  image: docker:20
  needs:
    - usermanager-maven-build
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker build -t $CI_REGISTRY_IMAGE/usermanager:$CI_COMMIT_REF_SLUG -f usermanager/Dockerfile .
    - docker push $CI_REGISTRY_IMAGE/usermanager:$CI_COMMIT_REF_SLUG
    - |
      if [[ "$CI_COMMIT_REF_NAME" == "test" ]]; then
        docker tag $CI_REGISTRY_IMAGE/usermanager:$CI_COMMIT_REF_SLUG $CI_REGISTRY_IMAGE/usermanager:test
        docker push $CI_REGISTRY_IMAGE/usermanager:test
      fi
    - |
      if [[ "$CI_COMMIT_REF_NAME" == "master" ]]; then
        docker tag $CI_REGISTRY_IMAGE/usermanager:$CI_COMMIT_REF_SLUG $CI_REGISTRY_IMAGE/usermanager:latest
        docker push $CI_REGISTRY_IMAGE/usermanager:latest
      fi
  only:
    - test
    - master

company-maven-build:
  <<: *maven-setup
  stage: build
  script:
    - mvn clean package -pl company -am -DskipTests
  artifacts:
    paths:
      - company/target/*.jar
      - core/target/*.jar
  dependencies:
    - company-test
    - core-build
  needs:
    - company-test
    - core-build
  only:
    - test
    - master

company-docker-build:
  stage: build
  image: docker:20
  needs:
    - company-maven-build
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker build -t $CI_REGISTRY_IMAGE/company:$CI_COMMIT_REF_SLUG -f company/Dockerfile .
    - docker push $CI_REGISTRY_IMAGE/company:$CI_COMMIT_REF_SLUG
    - |
      if [[ "$CI_COMMIT_REF_NAME" == "test" ]]; then
        docker tag $CI_REGISTRY_IMAGE/company:$CI_COMMIT_REF_SLUG $CI_REGISTRY_IMAGE/company:test
        docker push $CI_REGISTRY_IMAGE/company:test
      fi
    - |
      if [[ "$CI_COMMIT_REF_NAME" == "master" ]]; then
        docker tag $CI_REGISTRY_IMAGE/company:$CI_COMMIT_REF_SLUG $CI_REGISTRY_IMAGE/company:latest
        docker push $CI_REGISTRY_IMAGE/company:latest
      fi
  only:
    - test
    - master

# Automātiska izvietošana testa vidē
usermanager-deploy-test:
  stage: deploy-test
  image: docker:20
  needs:
    - job: usermanager-docker-build
      optional: true
  script:
    - apk add --no-cache openssh-client
    - mkdir -p ~/.ssh
    - echo "$TEST_SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
    - chmod 600 ~/.ssh/id_rsa
    - |
      ssh -o StrictHostKeyChecking=no $TEST_SERVER_USER@$TEST_SERVER "
        mkdir -p /home/dockeruser/degra/test/usermanager &&
        cd /home/dockeruser/degra/test/usermanager &&
        echo "$TEST_ENV_USERMANAGER" > .env &&
        cat > docker-compose.yml << 'EOL'
        version: '3'
        services:
          usermanager:
            image: $CI_REGISTRY_IMAGE/usermanager:$CI_COMMIT_REF_SLUG
            environment:
              - SPRING_DATASOURCE_URL=\${SPRING_DATASOURCE_URL}
              - SPRING_DATASOURCE_USERNAME=\${POSTGRES_USER}
              - SPRING_DATASOURCE_PASSWORD=\${POSTGRES_PASSWORD}
              - SPRING_JPA_HIBERNATE_DDL_AUTO=update
              - KEYCLOAK_AUTH_SERVER_URL=\${KEYCLOAK_AUTH_SERVER_URL}
              - KEYCLOAK_REALM=\${KEYCLOAK_REALM}
              - KEYCLOAK_RESOURCE=\${KEYCLOAK_RESOURCE}
              - KEYCLOAK_USE_RESOURCE_ROLE_MAPPINGS=true
              - SPRING_PROFILES_ACTIVE=\${SPRING_PROFILES_ACTIVE}
              - APP_SECURITY_ALLOWED_ORIGINS=\${APP_SECURITY_ALLOWED_ORIGINS}
            ports:
              - \"\${APP_PORT}:8080\"
            volumes:
              - ./logs:/logs
            restart: unless-stopped
      EOL
      "
      - |
      ssh -o StrictHostKeyChecking=no $TEST_SERVER_USER@$TEST_SERVER "
        cd /home/dockeruser/degra/test/usermanager &&
        docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY &&
        docker-compose pull &&
        docker-compose up -d
      "
  dependencies:
    - usermanager-docker-build
  environment:
    name: test-usermanager
    url: https://test.degra.lv/usermanager
  rules:
    - if: '$CI_COMMIT_BRANCH == "test"'
      when: always
    - if: '$CI_COMMIT_BRANCH != "test" && $CI_COMMIT_BRANCH != "master"'
      when: manual

company-deploy-test:
  stage: deploy-test
  image: docker:20
  needs:
    - job: company-docker-build
      optional: true
  script:
    - apk add --no-cache openssh-client
    - mkdir -p ~/.ssh
    - echo "$TEST_SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
    - chmod 600 ~/.ssh/id_rsa
    - |
      ssh -o StrictHostKeyChecking=no $TEST_SERVER_USER@$TEST_SERVER "
        mkdir -p /home/dockeruser/degra/test/company &&
        cd /home/dockeruser/degra/test/company &&
        echo "$TEST_ENV_COMPANY" > .env &&
        cat > docker-compose.yml << 'EOL'
        version: '3'
        services:
          company:
            image: $CI_REGISTRY_IMAGE/company:$CI_COMMIT_REF_SLUG
            environment:
              - SPRING_DATASOURCE_URL=\${SPRING_DATASOURCE_URL}
              - SPRING_DATASOURCE_USERNAME=\${POSTGRES_USER}
              - SPRING_DATASOURCE_PASSWORD=\${POSTGRES_PASSWORD}
              - SPRING_JPA_HIBERNATE_DDL_AUTO=update
              - KEYCLOAK_AUTH_SERVER_URL=\${KEYCLOAK_AUTH_SERVER_URL}
              - KEYCLOAK_REALM=\${KEYCLOAK_REALM}
              - KEYCLOAK_RESOURCE=\${KEYCLOAK_RESOURCE}
              - KEYCLOAK_USE_RESOURCE_ROLE_MAPPINGS=true
              - SPRING_PROFILES_ACTIVE=\${SPRING_PROFILES_ACTIVE}
              - APP_SECURITY_ALLOWED_ORIGINS=\${APP_SECURITY_ALLOWED_ORIGINS}
            ports:
              - \"\${APP_PORT}:8085\"
            volumes:
              - ./logs:/logs
            restart: unless-stopped
      "
      - |
      ssh -o StrictHostKeyChecking=no $TEST_SERVER_USER@$TEST_SERVER "
        cd /home/dockeruser/degra/test/company &&
        docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY &&
        docker-compose pull &&
        docker-compose up -d
      "
      

  dependencies:
    - company-docker-build
  environment:
    name: test-company
    url: https://test.degra.lv/company
  rules:
    - if: '$CI_COMMIT_BRANCH == "test"'
      when: always
    - if: '$CI_COMMIT_BRANCH != "test" && $CI_COMMIT_BRANCH != "master"'
      when: manual


# Izvietošana produkcijas vidē
usermanager-deploy-prod:
  stage: deploy-prod
  image: docker:20
  needs:
    - job: usermanager-docker-build
      optional: true
  script:
    - apk add --no-cache openssh-client
    - mkdir -p ~/.ssh
    - echo "$PROD_SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
    - chmod 600 ~/.ssh/id_rsa
    - |
      ssh -o StrictHostKeyChecking=no $PROD_SERVER_USER@$PROD_SERVER "
        mkdir -p /home/dockeruser/degra/prod/usermanager &&
        cd /home/dockeruser/degra/prod/usermanager &&
        echo "$PROD_ENV_USERMANAGER" > .env &&
        cat > docker-compose.yml << 'EOL'
        version: '3'
        services:
          usermanager:
            image: $CI_REGISTRY_IMAGE/usermanager:latest
            environment:
              - SPRING_DATASOURCE_URL=\${SPRING_DATASOURCE_URL}
              - SPRING_DATASOURCE_USERNAME=\${POSTGRES_USER}
              - SPRING_DATASOURCE_PASSWORD=\${POSTGRES_PASSWORD}
              - SPRING_JPA_HIBERNATE_DDL_AUTO=update
              - KEYCLOAK_AUTH_SERVER_URL=\${KEYCLOAK_AUTH_SERVER_URL}
              - KEYCLOAK_REALM=\${KEYCLOAK_REALM}
              - KEYCLOAK_RESOURCE=\${KEYCLOAK_RESOURCE}
              - KEYCLOAK_USE_RESOURCE_ROLE_MAPPINGS=true
              - SPRING_PROFILES_ACTIVE=\${SPRING_PROFILES_ACTIVE}
              - APP_SECURITY_ALLOWED_ORIGINS=\${APP_SECURITY_ALLOWED_ORIGINS}
            ports:
              - \"\${APP_PORT}:8080\"
            volumes:
              - ./logs:/logs
            restart: unless-stopped
      EOL
      "
      - |
      ssh -o StrictHostKeyChecking=no $TEST_SERVER_USER@$TEST_SERVER "
        cd /home/dockeruser/degra/prod/usermanager &&      
        docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY &&
        docker-compose pull &&
        docker-compose up -d
      "
      
      

  dependencies:
    - usermanager-docker-build
  environment:
    name: production-usermanager
    url: https://degra.lv/usermanager
  rules:
    - if: '$CI_COMMIT_BRANCH == "master"'
      when: manual

company-deploy-prod:
  stage: deploy-prod
  image: docker:20
  needs:
    - job: company-docker-build
      optional: true
  script:
    - apk add --no-cache openssh-client
    - mkdir -p ~/.ssh
    - echo "$PROD_SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
    - chmod 600 ~/.ssh/id_rsa
    - |
      ssh -o StrictHostKeyChecking=no $PROD_SERVER_USER@$PROD_SERVER "
        mkdir -p /home/dockeruser/degra/prod/company &&
        cd /home/dockeruser/degra/prod/company &&
        echo "$PROD_ENV_COMPANY" > .env &&
        cat > docker-compose.yml << 'EOL'
        version: '3'
        services:
          company:
            image: $CI_REGISTRY_IMAGE/company:latest
            environment:
              - SPRING_DATASOURCE_URL=\${SPRING_DATASOURCE_URL}
              - SPRING_DATASOURCE_USERNAME=\${POSTGRES_USER}
              - SPRING_DATASOURCE_PASSWORD=\${POSTGRES_PASSWORD}
              - SPRING_JPA_HIBERNATE_DDL_AUTO=update
              - KEYCLOAK_AUTH_SERVER_URL=\${KEYCLOAK_AUTH_SERVER_URL}
              - KEYCLOAK_REALM=\${KEYCLOAK_REALM}
              - KEYCLOAK_RESOURCE=\${KEYCLOAK_RESOURCE}
              - KEYCLOAK_USE_RESOURCE_ROLE_MAPPINGS=true
              - SPRING_PROFILES_ACTIVE=\${SPRING_PROFILES_ACTIVE}
              - APP_SECURITY_ALLOWED_ORIGINS=\${APP_SECURITY_ALLOWED_ORIGINS}
            ports:
              - \"\${APP_PORT}:8085\"
            volumes:
              - ./logs:/logs
            restart: unless-stopped
      EOL
      "
      - |
      ssh -o StrictHostKeyChecking=no $TEST_SERVER_USER@$TEST_SERVER "
        cd /home/dockeruser/degra/prod/company &&      
        docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY &&
        docker-compose pull &&
        docker-compose up -d
      "
      

  dependencies:
    - company-docker-build
  environment:
    name: production-company
    url: https://degra.lv/company
  rules:
    - if: '$CI_COMMIT_BRANCH == "master"'
      when: manual
